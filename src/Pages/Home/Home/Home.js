import React from 'react';
import Messages from '../Messages/Messages';
import Posts from '../Posts/Posts';

const Home = () => {
    return (
        <div>
            <Messages />
            <Posts />
        </div>
    );
};

export default Home;